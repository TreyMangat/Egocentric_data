# Labels

`accepted/` stores small, Git-tracked label documents by episode. Automatic text-rule labels are accepted for training by default; a manual edit replaces the automatic value while preserving its origin.

Each time range also stores its exact frame range, so it links directly to the matching RGB, wrist, hand, and keypoint data without duplicating those arrays.
