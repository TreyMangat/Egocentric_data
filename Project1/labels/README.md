# Labels

`manual/` stores small, Git-tracked label documents by episode. Automatic suggestions have `reviewed: false`; future training should use only labels you have reviewed.

Each time range also stores its exact frame range, so it links directly to the matching RGB, wrist, hand, and keypoint data without duplicating those arrays.
